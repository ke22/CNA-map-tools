# 快速上傳指南（5分鐘）

## 🎯 最簡單的方法：網頁上傳

### 步驟 1: 登錄 Mapbox Studio

訪問：**https://studio.mapbox.com/tilesets/**

### 步驟 2: 上傳第一個文件

1. 點擊 "**New tileset**" 按鈕（右上角）
2. 選擇 "**Upload file**"
3. 選擇文件：`data/gadm/tiles/gadm_level0.mbtiles`
4. 等待上傳和處理完成

### 步驟 3: 記錄 Tileset ID

上傳完成後，記下：
- Tileset ID: `your-username.gadm-level0`

### 步驟 4: 重複上傳其他級別

重複步驟 2-3，上傳：
- `gadm_level1.mbtiles` → `your-username.gadm-level1`
- `gadm_level2.mbtiles` → `your-username.gadm-level2`

---

## ⏱️ 時間估算

| 文件 | 大小 | 上傳時間 | 處理時間 | 總計 |
|------|------|---------|---------|------|
| Level 0 | 8.4 GB | 20-40 分鐘 | 30-60 分鐘 | 1-2 小時 |
| Level 1 | 6.4 GB | 15-30 分鐘 | 20-40 分鐘 | 1 小時 |
| Level 2 | 6.6 GB | 15-30 分鐘 | 20-40 分鐘 | 1 小時 |

**總計：約 3-5 小時**（可以同時上傳多個文件）

---

## ✅ 上傳完成後

記錄這三個 Tileset ID：
1. `your-username.gadm-level0`
2. `your-username.gadm-level1`
3. `your-username.gadm-level2`

然後更新 `js/app-vector-tiles.js` 中的配置。

---

## 💡 提示

- 上傳過程可以最小化瀏覽器，不會中斷
- 可以同時在多個瀏覽器標籤頁上傳不同文件
- 處理完成後會收到通知（如果設置了）


