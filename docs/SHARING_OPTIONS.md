# 分享地图工具 - 完整指南

## 🎯 分享方式对比

| 方式 | 适合场景 | 优点 | 缺点 | 难度 |
|------|---------|------|------|------|
| **GitHub Pages** | 快速演示 | 免费、无需服务器 | AI功能不可用 | ⭐ 简单 |
| **ngrok** | 临时演示 | 功能完整、快速 | 链接会过期 | ⭐⭐ 简单 |
| **GitHub 仓库** | 开发者协作 | 版本控制、完整 | 需要配置 | ⭐⭐⭐ 中等 |
| **局域网服务器** | 团队使用 | 功能完整、快速 | 只能局域网 | ⭐⭐ 简单 |
| **在线部署** | 长期使用 | 稳定、可访问 | 需要服务器 | ⭐⭐⭐⭐ 较难 |

---

## 🌐 方式 1: GitHub Pages（最简单）

### 适合谁？
- 想快速演示给他人
- 不需要 AI 功能
- 只展示地图和标注功能

### 步骤：

**1. 启用 GitHub Pages**
```
GitHub 仓库 → Settings → Pages
Source: Deploy from a branch
Branch: main 或 feature/genai
Folder: / (root)
```

**2. 分享链接**
```
https://ke22.github.io/CNA-map-tools/
```

**3. 需要使用者配置：**
- Mapbox Token（在 config.js 中，或提供说明让他们配置）

### 优点：
- ✅ 完全免费
- ✅ 无需服务器
- ✅ 自动更新

### 缺点：
- ❌ AI 功能不可用（需要后端）
- ❌ 需要公开仓库（或付费 GitHub）

---

## 🚀 方式 2: ngrok（临时公共链接）

### 适合谁？
- 需要完整功能演示
- 临时分享（几小时或几天）

### 步骤：

**1. 安装 ngrok**
```bash
# Mac
brew install ngrok

# 或从官网下载
# https://ngrok.com/download
```

**2. 启动本地服务器**
```bash
cd /Users/yulincho/Documents/GitHub/map
node server-combined.js
```

**3. 创建公共链接**
```bash
ngrok http 8000
```

**4. 分享生成的 URL**
```
例如：https://abc123.ngrok.io
```

### 优点：
- ✅ 功能完整（包括 AI）
- ✅ 可以分享给任何人
- ✅ 设置简单

### 缺点：
- ⏰ 免费版链接会过期
- 🔄 每次启动 URL 都变化
- 📊 有流量限制

### 使用稳定链接（付费版）：
```bash
ngrok http 8000 --domain=your-custom-domain.ngrok.io
```

---

## 🏠 方式 3: 局域网分享

### 适合谁？
- 同一办公室/团队
- 同一 Wi-Fi 网络

### 步骤：

**1. 查找你的 IP 地址**
```bash
# Mac/Linux
ifconfig | grep "inet " | grep -v 127.0.0.1

# Windows
ipconfig
```

**2. 启动服务器**
```bash
node server-combined.js
```

**3. 分享链接**
```
http://你的IP:8000
例如：http://192.168.1.100:8000
```

### 优点：
- ✅ 功能完整
- ✅ 速度快
- ✅ 无需配置

### 缺点：
- ❌ 只能局域网访问
- ❌ 需要知道 IP 地址

---

## 📦 方式 4: 打包分享代码

### 适合谁？
- 给开发者分享
- 需要自定义配置

### 创建分享包：

**1. 创建分享脚本**
```bash
#!/bin/bash
# create-share-package.sh

# 创建分享目录
mkdir -p map-tool-share
cd map-tool-share

# 复制必需文件
cp ../index-enhanced.html .
cp -r ../js .
cp -r ../css .
cp ../config.js .
cp ../server-combined.js .
cp ../server-gemini-proxy.js .
cp ../package.json* . 2>/dev/null || true

# 复制文档
cp ../SHARING_OPTIONS.md .
cp ../README.md .
cp ../.env.example .

# 复制启动脚本
cp ../START_SERVER.sh .
cp ../SETUP_API_KEY.sh .
chmod +x *.sh

# 创建使用说明
cat > QUICK_START.md << 'EOF'
# 快速开始

1. 设置 API 密钥：
   ./SETUP_API_KEY.sh

2. 启动服务器：
   ./START_SERVER.sh

3. 打开浏览器：
   http://localhost:8000
EOF

# 打包
cd ..
zip -r map-tool-share.zip map-tool-share/ -x "*.log" "node_modules/*" "data/gadm/*.geojson"

echo "✅ 分享包已创建：map-tool-share.zip"
```

---

## 🌍 方式 5: 在线部署（长期使用）

### 选项 A: Vercel / Netlify
- 适合：前端部署
- 限制：需要处理后端代理

### 选项 B: Railway / Render
- 适合：完整应用（前端+后端）
- 优点：简单配置、自动部署

### 选项 C: 自有服务器
- 适合：完全控制
- 需要：服务器、域名、SSL 证书

---

## 📋 分享前检查清单

### 代码检查：
- [ ] 已移除真实的 API 密钥
- [ ] 已创建 `.env.example` 模板
- [ ] 已更新 `.gitignore` 防止泄露
- [ ] 已测试所有功能正常

### 文档检查：
- [ ] 已创建 README.md
- [ ] 已创建快速开始指南
- [ ] 已说明需要的配置

### 安全检查：
- [ ] API 密钥不会泄露
- [ ] 敏感信息已移除
- [ ] 分享方式安全

---

## 🎁 快速分享包内容

```
map-tool-share/
├── index-enhanced.html      # 主文件
├── config.js                # 配置文件（需要填入 Mapbox Token）
├── js/                      # JavaScript 文件
├── css/                     # CSS 文件
├── server-combined.js       # 服务器（AI 功能需要）
├── server-gemini-proxy.js   # Gemini 代理
├── .env.example             # 环境变量模板
├── SETUP_API_KEY.sh         # API 密钥设置脚本
├── START_SERVER.sh          # 启动脚本
├── README.md                # 使用说明
└── QUICK_START.md           # 快速开始
```

---

## 📝 给使用者的说明模板

### 对于非技术用户：

```
欢迎使用地图工具！

1. 点击链接打开：https://your-link.com
2. 开始使用地图功能
3. 如果需要 AI 功能，请联系管理员设置
```

### 对于开发者：

```
1. 克隆仓库：
   git clone https://github.com/ke22/CNA-map-tools.git
   cd CNA-map-tools

2. 设置 Mapbox Token：
   编辑 config.js，填入你的 Mapbox Token

3. 设置 Gemini API Key（可选，AI 功能）：
   cp .env.example .env
   编辑 .env 填入 GEMINI_API_KEY

4. 启动服务器：
   ./START_SERVER.sh

5. 打开浏览器：
   http://localhost:8000
```

---

## 🔧 创建一键启动脚本

我可以帮你创建一个完整的一键分享脚本，包括：
- 检查依赖
- 设置环境变量
- 启动服务器
- 显示访问链接

需要我创建吗？



